name: Version

on:
  push:
    branches: [alpha, main]

jobs:
  version:
    runs-on: blacksmith-4vcpu-ubuntu-2404
    # Skip if the push was from this workflow (prevent infinite loops)
    if: "!startsWith(github.event.head_commit.message, 'chore: bump version')"
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Determine version bump
        id: bump
        run: |
          COMMIT_MSG=$(git log -1 --pretty=%B)
          echo "Commit message: $COMMIT_MSG"

          # Extract conventional commit type (e.g. "feat", "fix!",  "feat(scope)")
          TYPE=$(echo "$COMMIT_MSG" | head -1 | sed -n 's/^\([a-z]*\)\(([^)]*)\)\?!*:.*/\1/p')
          BANG=$(echo "$COMMIT_MSG" | head -1 | grep -c '!:' || true)
          BREAKING=$(echo "$COMMIT_MSG" | grep -c 'BREAKING CHANGE' || true)

          echo "Type: $TYPE, Bang: $BANG, Breaking: $BREAKING"

          if [ "$BREAKING" -gt 0 ] || [ "$BANG" -gt 0 ]; then
            echo "bump=major" >> "$GITHUB_OUTPUT"
          elif [ "$TYPE" = "feat" ]; then
            echo "bump=minor" >> "$GITHUB_OUTPUT"
          else
            # Every other push (fix, refactor, chore, docs, test, etc.) gets a patch bump
            echo "bump=patch" >> "$GITHUB_OUTPUT"
          fi

      - name: Bump version
        run: |
          BUMP="${{ steps.bump.outputs.bump }}"

          # Read current version from root plugin.json
          CURRENT=$(jq -r '.version' .claude-plugin/plugin.json)
          echo "Current version: $CURRENT"

          # Split into major.minor.patch
          IFS='.' read -r MAJOR MINOR PATCH <<< "$CURRENT"

          # Compute new version
          case "$BUMP" in
            major)
              MAJOR=$((MAJOR + 1))
              MINOR=0
              PATCH=0
              ;;
            minor)
              MINOR=$((MINOR + 1))
              PATCH=0
              ;;
            patch)
              PATCH=$((PATCH + 1))
              ;;
          esac

          NEW_VERSION="${MAJOR}.${MINOR}.${PATCH}"
          echo "New version: $NEW_VERSION"

          # Update root plugin.json
          jq --arg v "$NEW_VERSION" '.version = $v' \
            .claude-plugin/plugin.json > tmp.json && mv tmp.json .claude-plugin/plugin.json

          # Update ALL plugins in marketplace.json
          jq --arg v "$NEW_VERSION" '.plugins |= map(.version = $v)' \
            .claude-plugin/marketplace.json > tmp.json && mv tmp.json .claude-plugin/marketplace.json

          # Update individual plugin manifests
          for manifest in plugins/*/.claude-plugin/plugin.json; do
            if [ -f "$manifest" ]; then
              jq --arg v "$NEW_VERSION" '.version = $v' "$manifest" > tmp.json && mv tmp.json "$manifest"
            fi
          done

          # Update Cargo.toml versions
          for cargo in crates/*/Cargo.toml; do
            if [ -f "$cargo" ]; then
              sed -i "s/^version = \".*\"/version = \"${NEW_VERSION}\"/" "$cargo"
            fi
          done

          # Commit and tag
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "chore: bump version to ${NEW_VERSION}"
          git tag "v${NEW_VERSION}"
          git push origin HEAD --follow-tags
